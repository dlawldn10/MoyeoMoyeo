<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/img_login_bg"
    android:orientation="vertical"
    android:padding="32dp"
    tools:context=".LoginActivity">


    <TextView
        android:id="@+id/title_Login"
        android:layout_width="match_parent"
        android:layout_height="90dp"
        android:layout_marginTop="160dp"
        android:layout_marginBottom="-25dp"
        android:fontFamily="@font/notosans_regular"
        android:gravity="center"
        android:text="빛나는 우리들의 &#10; 재능을 위해"
        android:textColor="#ffffff"
        android:textSize="24dp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="90dp"
        android:fontFamily="@font/notosans_bold"
        android:gravity="center"
        android:text="WEMENTOR"
        android:textColor="#ffffff"
        android:textSize="36dp" />


    <ImageButton
        android:id="@+id/Login_Button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#00FFFFFF"
        android:scaleType="fitCenter"
        android:src="@drawable/btn_login"
        tools:ignore="SpeakableTextPresentCheck" />

    <ImageButton
        android:id="@+id/Kakao_Login_Bttn"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:background="#00FFFFFF"
        android:scaleType="fitCenter"
        android:src="@drawable/btn_kakao_login_medium_wide"
        tools:ignore="SpeakableTextPresentCheck"
        android:theme="@style/KakaoRipple" />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:fontFamily="@font/notosans_regular"
            android:gravity="center"
            android:text="기업 관계자 또는 멘토분께서는 여기를 눌러주세요"
            android:textColor="#ffffff"
            android:textSize="12dp" />

    </RelativeLayout>

</LinearLayout>